{
  "attention": {
    "window_size": 3
  },
  "batch_size": 16,
  "epochs": 16,
  "name": "default",
  "optimizer": {
    "momentum": 0.9,
    "learning_rate": 0.001,
    "type": "SGD",
    "weight_decay": 0
  },
  "rnn": {
    "dropout": 0,
    "hidden_size": 100,
    "num_layers": 1
  },
  "source_vocabulary_size": 10000,
  "target_vocabulary_size": 10000,
  "teacher_forcing": 0,
  "training": {
    "eval_every": 100,
    "sample_every": 200
  }
}
